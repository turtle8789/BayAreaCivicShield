// Encounter Log tab — re-exports the full log-list screen without a back button.
// The back-button inside log-list is harmlessly ignored when rendered as a tab.
export { default } from '../log-list';
